{
  "version": 2,
  "builds": [
    {
      "src": "angular.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/dfp-website" },
      "installCommand": "npm install"
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
